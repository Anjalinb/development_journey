int
float
decimal
char -- only alphabet
varchar -- alphabet and special chars
datetime
enum(choices) -- only allow the choices in the ()

CONSTARINTS
null
not null
unique
default
auto increment -- automatically increments
primary key
foreign key